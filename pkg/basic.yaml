debug:
  update_interval: 5s
esp32:
  board: esp32dev
  framework:
    type: arduino
# Enable logging
logger:
  level: debug
# Enable Home Assistant API
ota:
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password